#include <stdio.h>
int main (void)
{
	int m,n,i;
	double sum;
	sum=0;

	printf ("m=,n=\n");
    scanf("%d%d",&m,&n);
    for (i=m;i<=n;i++)
	{sum=i*i+1/i+sum;
	}
    printf ("%.6lf",&sum);
	return 0;
}
