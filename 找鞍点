#include<stdio.h>
void main ()
{
	int a[6][6],i,j,x,count,n,max2,p,k;
	scanf("%d",&n);
	count=0;
	for(i=0;i<n;i++)
		for(j=0;j<n;j++)
			scanf("%d",&a[i][j]);
	for(i=0;i<n;i++)
	{
	    for(j=0;j<n;j++)
		{
				for(k=1;k<n;k++)
				{
					if(a[i][k]>a[i][j])
						break;
					if(a[i][k]<=a[i][j])
					{
			            for(p=1;p<n;p++)
						{
					       if(a[p][j]<a[i][j])
						   break;
					       if(a[p][j]>=a[i][j])
						   {
						      count++;
						      x=a[i][j];
						   }
						}
					}
				}
		}
	}
	if(count>=2||count==0)
		printf("No");
	if(count==1)
		printf("%d",x);
}
