#include<stdio.h>
int main ()
{
	int a[10],b[10],i,j,n;
	printf("n=");
	scanf("%d",&n);
	for(i=0;i<n;i++)
		scanf("%d",&a[i]);
	j=0;
	for(i=0;i<n;i++)
		b[i]=a[n-1-i];
	for(i=0;i<n;i++)
	printf("%d",b[i]);
	return 0;
}
