#include <stdio.h>
int main ()
{
	int n,i,x,x1,min;
	printf ("n=");
	scanf ("%d",&n);
	printf("x1=");
	scanf("%d",&x1);
	min=x1;

	for(i=2;i<=n;i++)
	{
		printf ("x=");
		scanf ("%d",&x);
	if (x<min)
		min=x;
	}
	printf ("%d\n",min);
	return 0;


}
