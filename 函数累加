#include<stdio.h>
#include<math.h>
int fn(int a,int n);
int main ()
{
	int a,n,sum,item,A,i;
	sum=0;
	printf("a=");
	scanf("%d",&a);
	printf("n=");
	scanf("%d",&n);
	A=fn(a,n);
	for(i=1;i<=n;i++)
	{
		item=a*pow(10,i);
	    sum=sum+item;
	}
	printf("sum=%d",sum);
	return 0;

